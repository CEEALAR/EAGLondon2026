---
phase: 00-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ceealar-pulse/package.json
  - ceealar-pulse/app/layout.tsx
  - ceealar-pulse/app/page.tsx
  - ceealar-pulse/app/globals.css
  - ceealar-pulse/tailwind.config.ts
  - ceealar-pulse/components.json
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02

must_haves:
  truths:
    - "Next.js 15 App Router project exists at ./ceealar-pulse with TypeScript and Tailwind"
    - "All required npm packages are installed and appear in package.json"
    - "shadcn/ui is initialized with all 13 required components added"
    - "Home page renders 'CEEALAR Pulse — coming online' with brand colors"
    - "No Supabase imports or env var references exist in app/, components/, or lib/ — SDK is pre-installed in package.json for Phase 1 use"
  artifacts:
    - path: "ceealar-pulse/package.json"
      provides: "Dependency manifest"
      contains: "@supabase/supabase-js"
    - path: "ceealar-pulse/app/layout.tsx"
      provides: "Root layout with DM Sans font and brand CSS variables"
    - path: "ceealar-pulse/app/page.tsx"
      provides: "Placeholder home page"
    - path: "ceealar-pulse/app/globals.css"
      provides: "Tailwind base + brand color CSS variables"
    - path: "ceealar-pulse/components.json"
      provides: "shadcn/ui configuration"
  key_links:
    - from: "ceealar-pulse/app/layout.tsx"
      to: "ceealar-pulse/app/globals.css"
      via: "CSS import in layout"
      pattern: "globals\\.css"
    - from: "ceealar-pulse/app/page.tsx"
      to: "brand color variables"
      via: "Tailwind class or inline style using --color-teal or teal-700"
      pattern: "teal|#0F766E"
---

<objective>
Scaffold a new Next.js 15 App Router project, install all Phase 0 dependencies, initialize shadcn/ui with the required component set, and create a branded placeholder home page. No Supabase client is configured — env vars come in Phase 1.

Purpose: Establish the complete local codebase skeleton in a single pass so Plan 02 can push and deploy immediately.
Output: A runnable Next.js app at ./ceealar-pulse that shows "CEEALAR Pulse — coming online" in brand colors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project and install all dependencies</name>
  <files>
    ceealar-pulse/package.json
    ceealar-pulse/next.config.ts
    ceealar-pulse/tsconfig.json
    ceealar-pulse/tailwind.config.ts
    ceealar-pulse/postcss.config.mjs
  </files>
  <read_first>
    None — this is net-new. No existing files to read.
  </read_first>
  <action>
    Run the following commands in sequence from the directory CONTAINING the target folder (i.e., the EAG London working directory or wherever you want ceealar-pulse/ to live — confirm the parent directory first with `pwd`):

    1. Scaffold:
       npx create-next-app@latest ceealar-pulse --typescript --tailwind --app --no-src-dir --eslint --import-alias "@/*" --yes

    2. Move into the project:
       cd ceealar-pulse

    3. Install runtime dependencies (all at once):
       npm install @supabase/supabase-js @supabase/ssr xlsx date-fns lucide-react @tanstack/react-virtual

    4. Initialize shadcn/ui (accept defaults — style: default, base color: neutral, CSS variables: yes):
       npx shadcn@latest init --defaults

    5. Add all required shadcn components in one command:
       npx shadcn@latest add button input card badge dialog dropdown-menu textarea select sheet sonner tabs calendar popover checkbox

    Do NOT install: prettier, eslint plugins, husky, jest, cypress, storybook, zustand, prisma, or any package not listed above. Stack is locked.

    After these commands complete, verify package.json contains all of the following under "dependencies":
    - next (15.x)
    - react (19.x)
    - typescript (in devDependencies)
    - tailwindcss
    - @supabase/supabase-js
    - @supabase/ssr
    - xlsx
    - date-fns
    - lucide-react
    - @tanstack/react-virtual
  </action>
  <verify>
    <automated>cd ceealar-pulse && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; const required=['next','react','typescript','tailwindcss','@supabase/supabase-js','@supabase/ssr','xlsx','date-fns','lucide-react','@tanstack/react-virtual']; const missing=required.filter(d=>!deps[d]); if(missing.length){console.error('MISSING:',missing);process.exit(1);}else{console.log('All deps present');}"</automated>
  </verify>
  <acceptance_criteria>
    - ceealar-pulse/ directory exists with node_modules populated
    - package.json contains all 10 required packages listed above
    - components.json exists (shadcn/ui initialized)
    - ceealar-pulse/components/ui/ contains at minimum: button.tsx, input.tsx, card.tsx, badge.tsx, dialog.tsx
    - No TypeScript errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All dependencies installed, shadcn components present, TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Apply brand CSS variables and create placeholder home page</name>
  <files>
    ceealar-pulse/app/globals.css
    ceealar-pulse/app/layout.tsx
    ceealar-pulse/app/page.tsx
  </files>
  <read_first>
    Read ceealar-pulse/app/globals.css (generated by create-next-app) — understand the existing Tailwind @layer base block before editing.
    Read ceealar-pulse/app/layout.tsx — understand the existing font and metadata setup before replacing.
    Read ceealar-pulse/app/page.tsx — this will be fully replaced.
  </read_first>
  <action>
    BRAND SPEC:
    - Primary: teal-700 = #0F766E
    - Accent: gold-500 = #D4A017
    - Background: cream-50 = #FAF7F0
    - Type: DM Sans (body), Playfair Display italic (one accent per screen)
    - No emoji, no decorative icons

    Step 1 — globals.css:
    Append the following CSS custom properties into the existing :root block (or add a new :root block if none exists). Do NOT remove Tailwind directives (@tailwind base/components/utilities):

      :root {
        --color-teal: #0F766E;
        --color-gold: #D4A017;
        --color-cream: #FAF7F0;
      }

    Also set the body background to cream:
      body {
        background-color: var(--color-cream);
      }

    Step 2 — layout.tsx:
    Replace the font import(s) with DM Sans and Playfair Display from next/font/google. Preserve the existing metadata export (or improve it: title "CEEALAR Pulse", description "Conference coordination tool"). The className on <body> must include the DM Sans variable so it applies globally. Example import pattern (do not copy-paste blindly — adapt to the file's existing structure):

      import { DM_Sans, Playfair_Display } from 'next/font/google'

      const dmSans = DM_Sans({
        subsets: ['latin'],
        variable: '--font-dm-sans',
        display: 'swap',
      })

      const playfair = Playfair_Display({
        subsets: ['latin'],
        style: ['italic'],
        variable: '--font-playfair',
        display: 'swap',
      })

    Apply both variables to <body>:
      <body className={`${dmSans.variable} ${playfair.variable} font-sans`}>

    In tailwind.config.ts, extend the fontFamily to wire the CSS variables:
      fontFamily: {
        sans: ['var(--font-dm-sans)', 'DM Sans', 'sans-serif'],
        display: ['var(--font-playfair)', 'Playfair Display', 'serif'],
      }

    Step 3 — page.tsx:
    Replace the entire file with a minimal placeholder. Requirements:
    - No imports from Supabase, no env var references
    - Show the text "CEEALAR Pulse — coming online" as a heading
    - Apply brand colors: cream background (already on body), teal text for the heading
    - One Playfair Display italic accent — a subtitle or tagline (e.g. "Coordinating conversations that matter") using font-display italic
    - No other UI elements required — this is truly a placeholder

    Example structure (adapt freely — the content and styling matter, not the exact JSX):

      export default function Home() {
        return (
          <main className="min-h-screen flex flex-col items-center justify-center gap-4">
            <h1 className="text-3xl font-bold text-[#0F766E]">
              CEEALAR Pulse — coming online
            </h1>
            <p className="font-display italic text-[#D4A017] text-lg">
              Coordinating conversations that matter
            </p>
          </main>
        )
      }

    IMPORTANT: Do not add any dynamic data fetching, server actions, use client directive, or Supabase imports to page.tsx. Static JSX only.
  </action>
  <verify>
    <automated>cd ceealar-pulse && npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no compilation errors)
    - `npm run dev` starts without errors (check terminal output for "ready" or "Local:")
    - Visiting http://localhost:3000 in a browser shows "CEEALAR Pulse — coming online" in teal text on a cream background
    - The Playfair Display italic accent is visible as a subtitle
    - No Supabase-related imports appear anywhere in app/: `grep -r "supabase\|NEXT_PUBLIC_SUPABASE" ceealar-pulse/app/` returns no matches
    - globals.css contains --color-teal, --color-gold, --color-cream variables
  </acceptance_criteria>
  <done>Branded placeholder home page builds clean and renders correctly in browser. No Supabase references in Phase 0 code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → npm registry | Packages fetched from npm; supply chain risk on install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | npm install | accept | Phase 0 has no auth, no data, no secrets; supply chain risk is low for well-known packages |
| T-00-02 | Information Disclosure | page.tsx | accept | Static placeholder page exposes no PII or secrets; no env vars referenced |
</threat_model>

<verification>
After both tasks complete:
1. `cd ceealar-pulse && npm run build` exits 0
2. `npm run dev` serves on http://localhost:3000
3. Page shows "CEEALAR Pulse — coming online" in teal on cream background
4. No Supabase imports: `grep -r "supabase" ceealar-pulse/app/` returns nothing
5. All shadcn components exist: `ls ceealar-pulse/components/ui/` shows button.tsx, input.tsx, card.tsx, badge.tsx, dialog.tsx, checkbox.tsx (among others)
</verification>

<success_criteria>
- Next.js 15 App Router project scaffolded (BOOT-01)
- All dependencies installed including Supabase SDK, SheetJS, date-fns, lucide-react, TanStack Virtual, shadcn/ui with 13 components (BOOT-02)
- Branded placeholder page builds clean and renders in browser
- Zero Supabase client setup, zero env vars, zero tests — scope held
</success_criteria>

<output>
After completion, create `.planning/phases/00-bootstrap/00-01-SUMMARY.md` using the summary template.
</output>
