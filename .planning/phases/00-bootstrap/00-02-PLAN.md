---
phase: 00-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - ceealar-pulse/.gitignore
  - ceealar-pulse/.env.local
autonomous: false
requirements:
  - BOOT-03

must_haves:
  truths:
    - "ceealar-pulse/ is pushed to the GitHub repo Attila created"
    - "Vercel has deployed the commit and served a live https://*.vercel.app URL"
    - "The live URL renders 'CEEALAR Pulse — coming online' correctly"
    - "Executor has paused and shown the live URL to Attila before proceeding"
  artifacts:
    - path: "ceealar-pulse/.gitignore"
      provides: "Excludes .env.local, node_modules, .next from git"
      contains: ".env.local"
  key_links:
    - from: "GitHub repo (remote)"
      to: "Vercel project"
      via: "Vercel GitHub integration (auto-deploy on push)"
      pattern: "vercel.app"
---

<objective>
Push the scaffolded ceealar-pulse project to Attila's GitHub repo, trigger a Vercel deployment via the GitHub integration, confirm the live URL loads correctly, then STOP and present the URL to Attila before Phase 1 begins.

Purpose: Satisfy BOOT-03 — the app is publicly accessible at a Vercel URL.
Output: A live https://*.vercel.app URL serving the placeholder page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-bootstrap/00-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Collect GitHub remote URL from Attila</name>
  <what-built>
    Plan 01 produced a complete local Next.js project at ./ceealar-pulse. It is ready to push. The executor needs the GitHub remote URL to proceed.
  </what-built>
  <how-to-verify>
    Attila: please provide the GitHub remote URL for the repo you created.
    Format expected: https://github.com/CEEALAR/ceealar-pulse.git (or similar)

    Also confirm: Is the Vercel project already linked to this GitHub repo via the Vercel dashboard? (Yes/No)
    If not yet linked, the executor will guide you through that step after the push.
  </how-to-verify>
  <resume-signal>Paste the GitHub remote URL (e.g. https://github.com/CEEALAR/ceealar-pulse.git) and confirm Vercel link status.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Init git, add remote, commit, push</name>
  <files>
    ceealar-pulse/.gitignore
  </files>
  <read_first>
    Read ceealar-pulse/.gitignore — verify it already excludes .env.local, node_modules, and .next before committing. create-next-app generates this; confirm it is correct.
  </read_first>
  <action>
    Use the GitHub remote URL provided by Attila in Task 1.

    Run these commands from INSIDE the ceealar-pulse/ directory:

    1. Verify .gitignore contains the following entries (add if missing):
       - .env.local
       - .env*.local
       - node_modules/
       - .next/
       - out/

    2. Initialize git and make the first commit:
       git init
       git add .
       git commit -m "feat: scaffold ceealar-pulse — Phase 0 Bootstrap"

    3. Add the remote and push:
       git remote add origin <URL_FROM_ATTILA>
       git branch -M main
       git push -u origin main

    If the push fails due to the remote already having commits (e.g. a README from GitHub), pull and merge first:
       git pull origin main --allow-unrelated-histories
       git push -u origin main

    Do NOT push .env.local or any file containing credentials. The .gitignore from create-next-app should already handle this. Double-check with:
       git status --short

    If any .env* files appear in the staged list, unstage them immediately:
       git reset HEAD .env*
  </action>
  <verify>
    <automated>cd ceealar-pulse && git log --oneline -3 && git remote -v</automated>
  </verify>
  <acceptance_criteria>
    - `git remote -v` shows the GitHub origin URL provided by Attila
    - `git log --oneline -1` shows the bootstrap commit
    - `git status` shows "nothing to commit, working tree clean"
    - No .env.local appears in `git show HEAD --name-only`
    - GitHub repo page (https://github.com/...) shows the ceealar-pulse files in the main branch
  </acceptance_criteria>
  <done>Code pushed to GitHub main branch. Repository is clean — no secrets committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm Vercel deployment and present live URL — STOP GATE</name>
  <what-built>
    The push to GitHub triggers an automatic Vercel deployment (if the Vercel project is linked to the repo). The executor will retrieve the deployment URL and verify the page loads.

    After the push completes, the executor will:
    1. Wait up to 3 minutes for Vercel to build (check Vercel dashboard or use `vercel ls` if Vercel CLI is available)
    2. Identify the deployment URL (format: https://ceealar-pulse-*.vercel.app or the custom domain if configured)
    3. Confirm the page title and heading are correct using a HEAD/GET request

    STOP: This is the Phase 0 gate. Do not proceed to Phase 1 until Attila confirms.
  </what-built>
  <how-to-verify>
    Attila — please do the following:

    1. Open the live Vercel URL in your browser (the executor will provide it above this checkpoint)
    2. Confirm you see: "CEEALAR Pulse — coming online" in teal text on a cream background
    3. Confirm the page loads without browser console errors (open DevTools → Console)

    If the Vercel project is NOT yet linked to the GitHub repo:
    - Go to https://vercel.com/new
    - Import the GitHub repo you provided
    - Accept default settings (Next.js detected automatically)
    - Deploy
    - Provide the live URL here

    Once confirmed, also provide:
    - Supabase project URL (format: https://xxxx.supabase.co)
    - Supabase anon key (starts with eyJ...)
    - Supabase service role key (starts with eyJ...)
    - Google OAuth client ID
    - Google OAuth client secret

    These credentials will be used in Phase 1. The executor will NOT store them in any file — they go into Vercel environment variables only.
  </how-to-verify>
  <resume-signal>
    Type "confirmed — live URL: https://..." and paste the Vercel URL.
    Optionally paste the Supabase + Google credentials now to save time at Phase 1 start.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local → GitHub | Code pushed over HTTPS; credentials must not be committed |
| GitHub → Vercel | Auto-deploy webhook; Vercel builds from main branch |
| Vercel → public internet | Deployed app served publicly over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Information Disclosure | git commit | mitigate | Verify .gitignore excludes .env*.local before first commit; executor double-checks staged files |
| T-00-04 | Tampering | Vercel build | accept | Default Vercel build pipeline; no custom build scripts introduced in Phase 0 |
| T-00-05 | Information Disclosure | Vercel env vars (Phase 1) | mitigate | Credentials collected verbally at checkpoint, entered via Vercel dashboard — never committed to repo |
</threat_model>

<verification>
Phase 0 complete when:
1. GitHub repo main branch contains ceealar-pulse/ with all Phase 01 files
2. `git log --oneline -1` shows the bootstrap commit
3. Vercel deployment URL (https://*.vercel.app) returns HTTP 200
4. Page content includes "CEEALAR Pulse — coming online"
5. Attila has typed "confirmed" at the STOP gate
</verification>

<success_criteria>
- Placeholder home page deployed to Vercel with live URL confirmed (BOOT-03)
- Phase 0 STOP gate reached: executor paused, live URL presented to Attila
- Supabase + Google credentials collected for Phase 1 (out-of-band, not stored in repo)
</success_criteria>

<output>
After completion, create `.planning/phases/00-bootstrap/00-02-SUMMARY.md` using the summary template.
Include the live Vercel URL in the summary.
</output>
