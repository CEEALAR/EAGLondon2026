---
phase: 02-attendees
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - ceealar-pulse/app/(app)/attendees/[id]/page.tsx
  - ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx
autonomous: true
requirements:
  - ATT-05
  - ATT-06
  - ATT-07
  - ATT-08
  - ATT-09

must_haves:
  truths:
    - "Navigating to /attendees/[id] shows all 15 Swapcard fields for that attendee"
    - "The CEEALAR strategic context section shows 5 labeled textareas pre-filled with saved values"
    - "Editing a textarea and tabbing away (onBlur) saves the value to Supabase automatically"
    - "A subtle 'Saved' indicator appears after successful save and fades after 2 seconds"
    - "Tag chips section is present (stub — Phase 4 wires real tags)"
    - "Team meetings section is present (stub — Phase 3 wires real meetings)"
    - "'Schedule meeting' CTA button is present (stub — Phase 3 wires dialog)"
  artifacts:
    - path: "ceealar-pulse/app/(app)/attendees/[id]/page.tsx"
      provides: "Server page: fetch attendee by id, render all fields + strategic context form"
      exports: ["default AttendeeDetailPage"]
    - path: "ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx"
      provides: "Client component: 5 autosave textareas for strategic context fields"
      exports: ["StrategicContextForm"]
  key_links:
    - from: "app/(app)/attendees/[id]/page.tsx"
      to: "attendees/_components/strategic-context-form.tsx"
      via: "props: attendeeId={id} initialValues={strategicFields}"
      pattern: "StrategicContextForm"
    - from: "strategic-context-form.tsx"
      to: "supabase attendees table"
      via: "browser client update on blur"
      pattern: "from\\('attendees'\\)\\.update"
---

<objective>
Build the attendee detail page: server-rendered display of all 15 Swapcard fields plus client-side inline-editable strategic context with autosave on blur.

Purpose: Phase 2's core value proposition for the team — not just browsing attendees but adding CEEALAR-specific strategic thinking to each person's profile. Plan 02-03 is Wave 2 because it depends on the schema (02-01) and navigates from the list (02-02).
Output: Detail server page, strategic context autosave client form.
</objective>

<execution_context>
@C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH/Claude/EAG London/ceealar-pulse/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH\Claude\EAG London\.planning\ROADMAP.md

Phase 2 goal: Import 1,904 attendees from Swapcard XLSX, virtualized browse with search/filter, detail with editable strategic context.

Depends on:
- 02-01: attendees table schema and Attendee type
- 02-02: list page (navigation source for this detail page)

<interfaces>
<!-- Attendee type — from ceealar-pulse/lib/types.ts -->
export type Attendee = {
  id: string
  swapcard_url: string
  first_name: string | null
  last_name: string | null
  company: string | null
  job_title: string | null
  career_stage: string | null
  biography: string | null
  expertise: string[] | null
  interests: string[] | null
  how_others_can_help: string | null
  how_i_can_help: string | null
  country: string | null
  seeking_work: boolean | null
  recruitment: string | null
  linkedin: string | null
  why_they_matter: string | null
  how_to_engage: string | null
  hypothesis: string | null
  risks: string | null
  collaboration_hooks: string | null
  created_at: string
  updated_at: string
}

<!-- Server Supabase client — lib/supabase/server.ts -->
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()
const { data } = await supabase.from('attendees').select('*').eq('id', id).single()

<!-- Browser Supabase client — lib/supabase/client.ts -->
import { createClient } from '@/lib/supabase/client'  // (uses createBrowserClient)
// Usage in Client Component (non-async):
const supabase = createClient()
await supabase.from('attendees').update({ field: value }).eq('id', attendeeId)

<!-- (app) layout: content renders with md:pt-14 pb-16 md:pb-0 from layout.tsx -->
<!-- Available shadcn: Badge, Button, Textarea, all already installed -->
<!-- Available lucide icons: ExternalLink, MapPin, Briefcase, GraduationCap, Globe -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StrategicContextForm client component</name>
  <files>
    ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx as a Client Component ('use client').

    Props interface:
    - attendeeId: string
    - initialValues: { why_they_matter: string | null; how_to_engage: string | null; hypothesis: string | null; risks: string | null; collaboration_hooks: string | null }

    State:
    - const [values, setValues] = useState(initialValues) — initialized from props on mount
    - const [savedField, setSavedField] = useState<string | null>(null) — which field just saved (for indicator)

    Five field definitions (array to DRY the render loop):
    const FIELDS = [
      { key: 'why_they_matter', label: 'Why They Matter' },
      { key: 'how_to_engage', label: 'How to Engage' },
      { key: 'hypothesis', label: 'Hypothesis' },
      { key: 'risks', label: 'Risks' },
      { key: 'collaboration_hooks', label: 'Collaboration Hooks' },
    ] as const

    Type for key: type FieldKey = typeof FIELDS[number]['key']

    Save handler: const handleBlur = async (key: FieldKey, value: string) => {
      const supabase = createClient()  // from @/lib/supabase/client
      await supabase.from('attendees').update({ [key]: value || null }).eq('id', attendeeId)
      setSavedField(key)
      setTimeout(() => setSavedField(null), 2000)
    }

    Render each field:
    - label element with htmlFor matching textarea id, text-sm font-medium text-foreground
    - Textarea (shadcn, from @/components/ui/textarea) with id={field.key}, value={values[field.key] ?? ''}, onChange to update local values state, onBlur={() => handleBlur(field.key, values[field.key] ?? '')}, rows={3}, placeholder="Add notes..."
    - Inline "Saved" indicator: when savedField === field.key, show a span with text-xs text-green-600 "Saved" next to the label. Use a flex row between label and indicator.

    Section wrapper: div with className="space-y-4"
    Section heading (rendered by the parent page, not this component — do not add an h2 here).

    The component must NOT call useEffect to sync initialValues — props are passed once from server on page load and do not change. useState(initialValues) is sufficient.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&amp;1 | head -20</automated>
  </verify>
  <done>strategic-context-form.tsx exports StrategicContextForm. Five labeled textareas with onBlur autosave. "Saved" indicator fades after 2s. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Attendee detail server page</name>
  <files>
    ceealar-pulse/app/(app)/attendees/[id]/page.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/attendees/[id]/page.tsx as an async Server Component.

    Route params: props type { params: Promise<{ id: string }> } — Next.js 15 requires params to be awaited: const { id } = await props.params

    Data fetch: createClient() from server, then:
    const { data: attendee } = await supabase.from('attendees').select('*').eq('id', id).single()
    If attendee is null, call notFound() from 'next/navigation'.

    Page layout (max-w-2xl mx-auto px-4 py-6 space-y-8):

    Section 1 — Header:
    - h1: full name (first_name + last_name), fallback to swapcard_url. text-2xl font-bold
    - p: company + " · " + job_title (show only non-null parts). text-muted-foreground
    - p: career_stage if present. text-sm text-muted-foreground
    - "Schedule meeting" Button (variant="default") — renders as a disabled button with text "Schedule Meeting" and a comment {/* Phase 3: opens meeting create dialog */}. Import Button from @/components/ui/button.

    Section 2 — Swapcard Profile (all 15 fields):
    Render as a card-like div (border rounded-lg p-4 space-y-3 bg-card).
    h2: "Profile" text-lg font-semibold mb-3

    For each field, use a consistent pattern:
    - label (text-xs text-muted-foreground uppercase tracking-wide) + value (text-sm text-foreground)
    - If value is null/empty, show "-" as the value

    Fields to display (in this order):
    1. Biography — full text, allow wrapping (no truncation)
    2. Areas of Expertise — join expertise array with ", " or "-"
    3. Areas of Interest — join interests array with ", " or "-"
    4. How Others Can Help Me — how_others_can_help
    5. How I Can Help Others — how_i_can_help
    6. Country — country
    7. Career Stage — career_stage
    8. Seeking Work — seeking_work === true ? "Yes" : seeking_work === false ? "No" : "-"
    9. Recruitment — recruitment
    10. Swapcard — render as an anchor tag (href={attendee.swapcard_url}, target="_blank", rel="noopener noreferrer") with ExternalLink icon (lucide, size 12) inline. Text: "View on Swapcard"
    11. LinkedIn — same pattern as Swapcard but "View on LinkedIn". If null, show "-" (no link).

    Note: biography, how_others_can_help, how_i_can_help can be long — use whitespace-pre-wrap or just allow natural wrapping.

    Section 3 — CEEALAR Strategic Context:
    div with border rounded-lg p-4 bg-card
    h2: "CEEALAR Strategic Context" text-lg font-semibold mb-4
    Render StrategicContextForm with:
    - attendeeId={attendee.id}
    - initialValues={{ why_they_matter: attendee.why_they_matter, how_to_engage: attendee.how_to_engage, hypothesis: attendee.hypothesis, risks: attendee.risks, collaboration_hooks: attendee.collaboration_hooks }}

    Section 4 — Tags (stub):
    div with border rounded-lg p-4 bg-card
    h2: "Tags" text-lg font-semibold mb-2
    p with text-sm text-muted-foreground: "Tag management coming in Phase 4."
    Comment: {/* Phase 4: tag chips with add/remove */}

    Section 5 — Team Meetings (stub):
    div with border rounded-lg p-4 bg-card
    h2: "Team Meetings" text-lg font-semibold mb-2
    p with text-sm text-muted-foreground: "Meeting history coming in Phase 3."
    Comment: {/* Phase 3: list of meetings with this attendee */}

    Back navigation: add a Link (from 'next/link') at the top of the page above the header: href="/attendees", text "Back to attendees", text-sm text-muted-foreground with an arrow left icon or just "← Back".
  </action>
  <verify>
    <automated>npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&amp;1 | head -20</automated>
  </verify>
  <done>app/(app)/attendees/[id]/page.tsx is an async Server Component. Fetches attendee by id. Renders all 15 Swapcard fields. Renders StrategicContextForm. Stubs for Tags, Meetings, Schedule meeting button. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param [id] → Supabase query | User-controlled id used in DB lookup — must be validated by RLS |
| Browser client → Supabase update | Strategic context updates sent directly from browser — RLS must allow authenticated writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | /attendees/[id] server fetch | mitigate | (app) layout redirects unauthenticated users before page runs; RLS select policy requires auth.uid() is not null |
| T-02-09 | Tampering | Strategic context autosave | mitigate | RLS update policy requires auth.uid() is not null; browser client uses session cookie (anon key + session, not service role); user can only write if authenticated |
| T-02-10 | Tampering | [id] param injection | accept | Supabase parameterized queries via the JS client prevent SQL injection; invalid UUIDs return null (handled by notFound()) |
| T-02-11 | Information Disclosure | Strategic context fields | accept | All four team members are trusted equals (no per-user access control needed); strategic context is team-shared by design |
</threat_model>

<verification>
After both tasks complete:

1. Detail page present: ls "ceealar-pulse/app/(app)/attendees/[id]/page.tsx"
2. Strategic form present: ls "ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx"
3. Autosave wired: grep -c "handleBlur\|onBlur" "ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx"
4. All 5 fields defined: grep -c "why_they_matter\|how_to_engage\|hypothesis\|risks\|collaboration_hooks" "ceealar-pulse/app/(app)/attendees/_components/strategic-context-form.tsx"
5. TypeScript compiles: npx tsc --noEmit --project ceealar-pulse/tsconfig.json
</verification>

<success_criteria>
- /attendees/[id] shows full name, all 15 Swapcard profile fields, strategic context section, tags stub, meetings stub, "Schedule meeting" button stub
- StrategicContextForm renders 5 labeled textareas pre-filled from DB
- onBlur triggers Supabase update via browser client; "Saved" indicator appears and disappears after 2s
- notFound() called for invalid attendee ids
- TypeScript compiles with no errors across the project
</success_criteria>

<output>
After completion, create `.planning/phases/02-attendees/02-03-SUMMARY.md` using the standard summary template.
</output>
