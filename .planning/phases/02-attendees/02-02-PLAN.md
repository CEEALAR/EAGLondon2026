---
phase: 02-attendees
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ceealar-pulse/app/(app)/attendees/page.tsx
  - ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx
  - ceealar-pulse/app/(app)/attendees/_components/attendee-row.tsx
autonomous: true
requirements:
  - ATT-01
  - ATT-02
  - ATT-03
  - ATT-04

must_haves:
  truths:
    - "User sees a virtualized list of all attendees without pagination"
    - "Each row shows initials avatar, full name, company and job title, top 2 expertise chips"
    - "Typing in the search bar filters the list by name or company within 200ms"
    - "Tapping the Filter button opens a Sheet drawer (stubs — active filtering wired in Phase 4)"
    - "List DOM stays lightweight even with 1,904 rows (only visible rows rendered)"
  artifacts:
    - path: "ceealar-pulse/app/(app)/attendees/page.tsx"
      provides: "Server component that fetches all attendees and passes to AttendeeList"
      exports: ["default AttendeesPage"]
    - path: "ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx"
      provides: "Client component with TanStack Virtual row rendering, search input, filter sheet"
      exports: ["AttendeeList"]
    - path: "ceealar-pulse/app/(app)/attendees/_components/attendee-row.tsx"
      provides: "Single virtualized row with initials avatar, name, company, expertise badges"
      exports: ["AttendeeRow"]
  key_links:
    - from: "app/(app)/attendees/page.tsx"
      to: "attendees/_components/attendee-list.tsx"
      via: "props: attendees={data}"
      pattern: "AttendeeList"
    - from: "attendee-list.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer hook"
      pattern: "useVirtualizer"
    - from: "attendee-list.tsx"
      to: "attendee-row.tsx"
      via: "virtualItems.map -> AttendeeRow"
      pattern: "AttendeeRow"
---

<objective>
Build the virtualized attendee browse experience: a server page that loads all attendees, a TanStack Virtual list with debounced search, and a stub filter sheet.

Purpose: This is the primary entry point for the app — 1,904 rows must render without lag. TanStack Virtual ensures only visible rows are in the DOM.
Output: Server page, virtual list client component, row component.
</objective>

<execution_context>
@C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH/Claude/EAG London/ceealar-pulse/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH\Claude\EAG London\.planning\ROADMAP.md

Phase 2 goal: Import 1,904 attendees from Swapcard XLSX, virtualized browse with search/filter, detail with editable strategic context.

<interfaces>
<!-- Attendee type — from ceealar-pulse/lib/types.ts (created in 02-01) -->
export type Attendee = {
  id: string
  swapcard_url: string
  first_name: string | null
  last_name: string | null
  company: string | null
  job_title: string | null
  career_stage: string | null
  biography: string | null
  expertise: string[] | null
  interests: string[] | null
  how_others_can_help: string | null
  how_i_can_help: string | null
  country: string | null
  seeking_work: boolean | null
  recruitment: string | null
  linkedin: string | null
  why_they_matter: string | null
  how_to_engage: string | null
  hypothesis: string | null
  risks: string | null
  collaboration_hooks: string | null
  created_at: string
  updated_at: string
}

<!-- Server Supabase client — lib/supabase/server.ts -->
import { createClient } from '@/lib/supabase/server'
// Usage in async server component:
const supabase = await createClient()
const { data, error } = await supabase.from('attendees').select('*').order('last_name')

<!-- (app) layout context: TopNav is 56px (md:pt-14 = 3.5rem). BottomTabBar is 64px (pb-16 = 4rem on mobile). -->
<!-- Available shadcn components: Badge, Input, Sheet, SheetContent, SheetHeader, SheetTitle, Button -->
<!-- TanStack Virtual installed: @tanstack/react-virtual ^3.13.24 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server page — fetch all attendees</name>
  <files>
    ceealar-pulse/app/(app)/attendees/page.tsx
  </files>
  <action>
    Replace the placeholder ceealar-pulse/app/(app)/attendees/page.tsx with a proper async Server Component.

    Implementation:
    - Import createClient from @/lib/supabase/server and Attendee type from @/lib/types
    - Import AttendeeList from ./_components/attendee-list (created in Task 2 — TypeScript will resolve at compile time)
    - Fetch: const supabase = await createClient(); const { data, error } = await supabase.from('attendees').select('*').order('last_name', { ascending: true })
    - If error or no data, pass an empty array to AttendeeList (do not throw — graceful empty state)
    - Render: return <AttendeeList attendees={data ?? []} />
    - No padding wrapper needed — AttendeeList manages its own layout including sticky search bar

    The page is a Server Component (no 'use client'). Data flows down as props to the Client Component list.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&amp;1 | head -20</automated>
  </verify>
  <done>page.tsx is an async Server Component, imports AttendeeList, fetches attendees from Supabase, passes data as prop. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: AttendeeRow component</name>
  <files>
    ceealar-pulse/app/(app)/attendees/_components/attendee-row.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/attendees/_components/attendee-row.tsx as a Client Component ('use client').

    Props interface:
    - attendee: Attendee
    - style: React.CSSProperties  (passed from TanStack Virtual for absolute positioning)

    Layout (height must match estimateSize: 72):
    - Outer div: style={style} with position absolute, width 100%, height 72px, flex row, items-center, px-4, gap-3, border-b border-border, cursor-pointer, hover:bg-muted/50 transition
    - Link wrapping the entire row to /attendees/{attendee.id} using Next.js Link (import from 'next/link')

    Initials avatar:
    - 40x40 circle, bg-[var(--color-teal)] text-white font-semibold text-sm, flex items-center justify-center, rounded-full, shrink-0
    - Initials: first letter of first_name + first letter of last_name (both uppercased). If either is null, use '?' as fallback for that letter.

    Name + company/job title (flex-1 min-w-0):
    - Full name: p tag, text-sm font-medium text-foreground, truncate. Format: "{first_name} {last_name}", fallback to swapcard_url if both are null.
    - Company/job title: p tag, text-xs text-muted-foreground, truncate. Format: "{company} · {job_title}". Show only the non-null parts joined with " · ". If both null, show empty string.

    Expertise chips (flex gap-1, shrink-0, max visible: 2):
    - Show attendee.expertise?.slice(0, 2) as Badge components (variant="secondary", text-xs)
    - Import Badge from @/components/ui/badge
    - If expertise is null or empty, render nothing in this slot

    Tag chips: render nothing (empty slot — Phase 4 wires real tags). Add a comment: {/* Phase 4: tag chips */}

    The row must be exactly 72px tall. The outer div has fixed height 72 and the content is vertically centered via flexbox. This is critical for TanStack Virtual's estimateSize to be correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&amp;1 | head -20</automated>
  </verify>
  <done>attendee-row.tsx exports AttendeeRow. Component renders 72px row with initials avatar, name, company/job, expertise badges. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: AttendeeList with TanStack Virtual, search, and filter stub</name>
  <files>
    ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx as a Client Component ('use client').

    Imports:
    - 'use client'
    - React, { useRef, useState, useMemo, useCallback, useEffect } from 'react'
    - { useVirtualizer } from '@tanstack/react-virtual'
    - { Attendee } from '@/lib/types'
    - { AttendeeRow } from './attendee-row'
    - { Input } from '@/components/ui/input'
    - { Button } from '@/components/ui/button'
    - { Sheet, SheetContent, SheetHeader, SheetTitle } from '@/components/ui/sheet'
    - { SlidersHorizontal } from 'lucide-react'

    Props: { attendees: Attendee[] }

    State:
    - const [query, setQuery] = useState('')
    - const [debouncedQuery, setDebouncedQuery] = useState('')
    - const [filterOpen, setFilterOpen] = useState(false)

    Debounce: useEffect that sets a 200ms timeout to update debouncedQuery from query. Clears timeout on cleanup.

    Filtered list (useMemo, depends on [attendees, debouncedQuery]):
    - If debouncedQuery is empty, return attendees unchanged
    - Otherwise: const q = debouncedQuery.toLowerCase(); return attendees.filter(a => [a.first_name, a.last_name, a.company].some(f => f?.toLowerCase().includes(q)))

    TanStack Virtual setup:
    - const parentRef = useRef<HTMLDivElement>(null)
    - const virtualizer = useVirtualizer({ count: filtered.length, getScrollElement: () => parentRef.current, estimateSize: () => 72, overscan: 10 })

    Layout (full-page flex column):
    - Outer wrapper: div with className "flex flex-col h-[calc(100vh-56px-64px)] md:h-[calc(100vh-56px)]"
      (56px = top nav; 64px = bottom tab bar on mobile; desktop has no bottom tab bar)
    - Search bar row: div with className "flex gap-2 px-4 py-2 sticky top-0 bg-background z-10 border-b border-border shrink-0"
      - Input placeholder="Search by name or company..." value={query} onChange={e => setQuery(e.target.value)} className="flex-1"
      - Button variant="outline" size="icon" onClick={() => setFilterOpen(true)} aria-label="Filters": render SlidersHorizontal icon (size 16)
    - Scrollable list container: div ref={parentRef} with className "flex-1 overflow-auto relative"
      - Inner height div: div style={{ height: virtualizer.getTotalSize() }} className="relative w-full"
        - Map virtualizer.getVirtualItems() to: AttendeeRow key={item.key} attendee={filtered[item.index]} style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: `${item.size}px`, transform: `translateY(${item.start}px)` }}

    Empty state: when filtered.length === 0 and debouncedQuery is empty, show centered text "No attendees imported yet — visit /admin/import". When filtered.length === 0 and debouncedQuery is non-empty, show "No results for '{debouncedQuery}'".

    Filter Sheet (stub):
    - Sheet open={filterOpen} onOpenChange={setFilterOpen}
    - SheetContent side="right" (desktop drawer) or side="bottom" (mobile). Use side="bottom" for simplicity — matches mobile-first design.
    - SheetHeader: SheetTitle "Filters"
    - Body: p with text-muted-foreground "Tag, expertise, interest, and country filters coming in Phase 4."
    - Close Button at bottom: variant="outline" onClick={() => setFilterOpen(false)} text "Close"
  </action>
  <verify>
    <automated>npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&amp;1 | head -20</automated>
  </verify>
  <done>attendee-list.tsx exports AttendeeList. TanStack Virtual renders only visible rows. Search debounces at 200ms. Filter sheet opens and closes. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → Server Component | DB query runs server-side with anon key + RLS — user must be authenticated (layout enforces this) |
| Server Component → Client Component | Attendee data serialized as JSON prop — all strings, no functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | /attendees page | mitigate | (app) layout redirects unauthenticated users to /signin before this page renders; RLS also blocks anon queries |
| T-02-06 | Tampering | Client-side search filter | accept | Search runs on already-fetched data; no server call; no injection vector since filter is a JS string comparison |
| T-02-07 | Denial of Service | Loading 1,904 rows in one query | accept | Team-internal tool with 4 users; ~1,904 rows is well within Supabase free tier and Next.js serialization limits |
</threat_model>

<verification>
After all three tasks complete:

1. Server page present: verify ceealar-pulse/app/(app)/attendees/page.tsx does NOT contain "Coming in Phase 2"
2. Components present: ls ceealar-pulse/app/(app)/attendees/_components/
3. TanStack Virtual wired: grep -c "useVirtualizer" "ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx"
4. Debounce present: grep -c "setTimeout" "ceealar-pulse/app/(app)/attendees/_components/attendee-list.tsx"
5. TypeScript compiles: npx tsc --noEmit --project ceealar-pulse/tsconfig.json
</verification>

<success_criteria>
- /attendees page is a Server Component that fetches all attendees and passes to AttendeeList
- AttendeeRow renders a 72px row with initials avatar, name, company/job title, up to 2 expertise badges
- AttendeeList uses useVirtualizer (estimateSize 72, overscan 10) — only visible rows in DOM
- Search input debounces 200ms and filters by name and company
- Filter button opens Sheet stub with "coming in Phase 4" message
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-attendees/02-02-SUMMARY.md` using the standard summary template.
</output>
