---
phase: 03-meetings-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ceealar-pulse/supabase/migrations/0003_meetings.sql
  - ceealar-pulse/lib/types.ts
  - ceealar-pulse/app/(app)/attendees/[id]/page.tsx
  - ceealar-pulse/app/(app)/attendees/_components/meeting-create-dialog.tsx
  - ceealar-pulse/app/(app)/meetings/[id]/page.tsx
autonomous: true
requirements:
  - MEET-01
  - MEET-02
  - MEET-03
  - MEET-04
  - MEET-05
  - MEET-06
  - MEET-07
  - MEET-08
  - MEET-09
  - ATT-08

must_haves:
  truths:
    - "meetings, action_items, and activity tables created with correct schema and RLS"
    - "Meeting create dialog opens from attendee detail page"
    - "'Want to meet' quick action creates meeting with status='want_to_meet'"
    - "/meetings/[id] shows attendee name, owner, status badge, time"
    - "Prep note hidden from non-owner when status != done"
    - "Summary, Meeting Notes, Comments autosave on blur"
    - "Action items: add, check off, delete"
    - "Status changer with confirmation"
    - "Activity rows written on: create, status->done, action item add, action item complete"
  artifacts:
    - path: "ceealar-pulse/supabase/migrations/0003_meetings.sql"
      provides: "meetings, action_items, activity tables + RLS + indexes + meetings_view"
    - path: "ceealar-pulse/lib/types.ts"
      provides: "Meeting, ActionItem, Activity TypeScript types added"
    - path: "ceealar-pulse/app/(app)/attendees/_components/meeting-create-dialog.tsx"
      provides: "MeetingCreateDialog client component"
    - path: "ceealar-pulse/app/(app)/attendees/[id]/page.tsx"
      provides: "Schedule Meeting button wired to dialog; Team Meetings section shows real data"
    - path: "ceealar-pulse/app/(app)/meetings/[id]/page.tsx"
      provides: "Full meeting detail page with notes, action items, status changer"
---

<objective>
Lay the data and UI foundation for Phase 3: create the meetings/action_items/activity tables,
define TypeScript types, build the meeting create dialog, and implement the full meeting detail page.

Purpose: This is the core of the entire Meetings + Notes phase. All MEET-01 through MEET-09
requirements are addressed here.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Supabase migration and TypeScript types</name>
  <files>
    ceealar-pulse/supabase/migrations/0003_meetings.sql
    ceealar-pulse/lib/types.ts
  </files>
  <action>
    Create the migration for the meetings phase.

    Create ceealar-pulse/supabase/migrations/0003_meetings.sql with:

    1. meetings table:
       - id uuid pk default gen_random_uuid()
       - attendee_id uuid references public.attendees(id) on delete cascade not null
       - owner_id uuid references public.team_members(id) on delete set null
       - status text not null default 'want_to_meet' check (status in ('want_to_meet','planned','done','no_show','cancelled'))
       - scheduled_at timestamptz
       - duration_minutes integer
       - location text
       - prep_note text  -- owner-private until status=done
       - summary text
       - meeting_notes text
       - comments text
       - follow_up_date date
       - created_at timestamptz not null default now()
       - updated_at timestamptz not null default now()

    2. action_items table:
       - id uuid pk default gen_random_uuid()
       - meeting_id uuid references public.meetings(id) on delete cascade not null
       - created_by uuid references public.team_members(id) on delete set null
       - text text not null
       - due_date date
       - done boolean not null default false
       - done_at timestamptz
       - created_at timestamptz not null default now()

    3. activity table:
       - id uuid pk default gen_random_uuid()
       - actor_id uuid references public.team_members(id) on delete set null
       - meeting_id uuid references public.meetings(id) on delete cascade
       - attendee_id uuid references public.attendees(id) on delete cascade
       - action text not null  -- 'meeting_created','status_done','action_item_added','action_item_completed'
       - detail jsonb
       - created_at timestamptz not null default now()

    4. meetings_view — a Postgres view that hides prep_note for non-owners when status != 'done':
       create or replace view public.meetings_view as
       select
         m.*,
         case
           when m.status = 'done' then m.prep_note
           when m.owner_id = auth.uid() then m.prep_note
           else null
         end as prep_note
       from public.meetings m;

       Note: This view replaces prep_note with null for non-owners. Components should query
       meetings_view, not meetings directly.

    5. RLS policies:
       - meetings: enable RLS. select/insert/update: auth.uid() is not null.
         delete: auth.uid() = owner_id.
       - action_items: enable RLS. select/insert: auth.uid() is not null.
         update/delete: auth.uid() is not null (all team members can manage action items).
       - activity: enable RLS. select/insert: auth.uid() is not null.

    6. updated_at trigger on meetings (reuse update_updated_at() function already created in 0002).

    7. Indexes:
       - meetings(attendee_id)
       - meetings(owner_id)
       - action_items(meeting_id)
       - activity(meeting_id)
       - activity(created_at desc) for feed ordering

    Update ceealar-pulse/lib/types.ts to add Meeting, ActionItem, Activity types:

    export type MeetingStatus = 'want_to_meet' | 'planned' | 'done' | 'no_show' | 'cancelled'

    export type Meeting = {
      id: string
      attendee_id: string
      owner_id: string | null
      status: MeetingStatus
      scheduled_at: string | null
      duration_minutes: number | null
      location: string | null
      prep_note: string | null  -- may be null for non-owners
      summary: string | null
      meeting_notes: string | null
      comments: string | null
      follow_up_date: string | null
      created_at: string
      updated_at: string
    }

    export type ActionItem = {
      id: string
      meeting_id: string
      created_by: string | null
      text: string
      due_date: string | null
      done: boolean
      done_at: string | null
      created_at: string
    }

    export type Activity = {
      id: string
      actor_id: string | null
      meeting_id: string | null
      attendee_id: string | null
      action: string
      detail: Record<string, unknown> | null
      created_at: string
    }

    export type TeamMember = {
      id: string
      email: string
      display_name: string | null
      avatar_url: string | null
      created_at: string
    }
  </action>
  <verify>
    grep -c "create table public.meetings" "ceealar-pulse/supabase/migrations/0003_meetings.sql"
  </verify>
  <done>Migration file contains meetings, action_items, activity tables + RLS + indexes + meetings_view. types.ts exports Meeting, ActionItem, Activity, TeamMember types.</done>
</task>

<task type="auto">
  <name>Task 2: Meeting create dialog</name>
  <files>
    ceealar-pulse/app/(app)/attendees/_components/meeting-create-dialog.tsx
    ceealar-pulse/app/(app)/attendees/[id]/page.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/attendees/_components/meeting-create-dialog.tsx as a Client Component.

    Props:
    - attendeeId: string
    - attendeeName: string
    - currentUserId: string
    - teamMembers: TeamMember[]  -- for owner dropdown

    State:
    - open: boolean
    - saving: boolean
    - owner_id: string (default: currentUserId)
    - status: MeetingStatus (default: 'want_to_meet')
    - scheduled_at: string (datetime-local input value)
    - duration_minutes: string (number input)
    - location: string

    UI: a shadcn Dialog with a trigger button "Schedule Meeting".
    Dialog title: "Schedule Meeting with {attendeeName}"
    
    Form fields (all optional except owner):
    - Owner dropdown (Select): shows team member display_name; defaults to current user
    - Status Select: want_to_meet, planned (show_time=true when planned)
    - Date+time input (datetime-local) — only shown when status='planned'
    - Duration input (number, minutes) — only shown when status='planned'
    - Location text input

    Two submit buttons:
    - "Want to Meet" — creates with status='want_to_meet', no time required; closes dialog
    - "Save Meeting" — creates with current status/time; closes dialog

    On submit:
    1. POST to /api/meetings with JSON body: { attendee_id, owner_id, status, scheduled_at, duration_minutes, location }
    2. If ok: close dialog, call router.refresh() to reload server component
    3. If error: show error toast via sonner

    Create ceealar-pulse/app/api/meetings/route.ts exporting POST:
    - Auth check: getUser(), 401 if no user
    - Parse JSON body
    - Insert into meetings table (NOT meetings_view — insert always goes to base table)
    - Write activity row: action='meeting_created', actor_id=user.id, meeting_id=new meeting id, attendee_id
    - Return 201 with the new meeting JSON

    Update ceealar-pulse/app/(app)/attendees/[id]/page.tsx:
    - Fetch team_members from Supabase for the owner dropdown
    - Fetch meetings for this attendee (join team_members for owner display_name) from meetings table
    - Pass currentUserId to MeetingCreateDialog
    - Replace the disabled "Schedule Meeting" button with <MeetingCreateDialog>
    - Replace the "Meeting history coming in Phase 3" stub with a real list of meetings:
      Show each meeting as a card with: owner display_name, status badge, scheduled_at (formatted with date-fns format), link to /meetings/[id]

    Color-code status badges:
    - want_to_meet: default/outline
    - planned: teal (bg-teal-100 text-teal-800)
    - done: gold (bg-yellow-100 text-yellow-800)
    - no_show/cancelled: muted

    Import MeetingCreateDialog as a client component inside the server page — this is fine in Next.js App Router.
  </action>
  <verify>
    npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&1 | head -30
  </verify>
  <done>MeetingCreateDialog component exists. Attendee detail page Schedule Meeting button is wired. Team meetings section shows real data. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Meeting detail page</name>
  <files>
    ceealar-pulse/app/(app)/meetings/[id]/page.tsx
    ceealar-pulse/app/(app)/meetings/[id]/_components/meeting-notes-form.tsx
    ceealar-pulse/app/(app)/meetings/[id]/_components/action-items-section.tsx
    ceealar-pulse/app/(app)/meetings/[id]/_components/status-changer.tsx
  </files>
  <action>
    Create ceealar-pulse/app/(app)/meetings/[id]/page.tsx as a server component.

    Fetches:
    - Meeting from meetings_view (to get prep_note visibility applied) joined with attendees and team_members
    - Action items from action_items table for this meeting
    - Current user id from auth

    Layout (mobile-first, max-w-2xl mx-auto px-4 py-6):

    Section 1 — Header:
    - Back link: "← Back to [attendee name]" linking to /attendees/[attendee_id]
    - Attendee name as h1 (linked to /attendees/[attendee_id])
    - Owner: "Meeting owned by {owner display_name}"
    - Status badge (color-coded same as above)
    - Scheduled time: formatted with date-fns format(parseISO(scheduled_at), "EEEE d MMM yyyy 'at' HH:mm") — or "Not scheduled" if null
    - Location: if set, show it
    - Follow-up date: if set, show "Follow up: {date}"

    Section 2 — Status Changer (client component):
    <StatusChanger meetingId={id} currentStatus={meeting.status} isOwner={currentUserId === meeting.owner_id} />

    Section 3 — Prep Note (only if currentUserId === meeting.owner_id OR meeting.status === 'done'):
    Label: "Prep Note (private until meeting is done)"
    Textarea with autosave on blur — PATCH /api/meetings/[id] with { prep_note }

    Section 4 — Meeting Log:
    Three labeled textareas (client component MeetingNotesForm):
    - Summary
    - Meeting Notes
    - Comments
    Each autosaves on blur via PATCH /api/meetings/[id] with the changed field.

    Section 5 — Action Items:
    <ActionItemsSection meetingId={id} initialItems={actionItems} currentUserId={currentUserId} />

    Section 6 — Follow-up Date Picker:
    Simple date input (type="date") that PATCHes /api/meetings/[id] with { follow_up_date } on change.

    Create /app/api/meetings/[id]/route.ts exporting PATCH:
    - Auth check
    - Parse JSON body — accept any subset of: prep_note, summary, meeting_notes, comments, follow_up_date, status
    - Update meetings set ... where id=? and (owner_id = auth.uid() for status/prep_note changes)
    - If status is being set to 'done': write activity row action='status_done'
    - Return updated meeting JSON

    Create meeting-notes-form.tsx as 'use client':
    - Props: meetingId, initialValues: {summary, meeting_notes, comments, prep_note, isOwner}
    - Three (or four if isOwner) Textarea fields
    - onBlur handler: PATCH /api/meetings/[id] with {fieldName: value}
    - Show "Saved" text briefly after successful save (use useState + setTimeout to clear)

    Create action-items-section.tsx as 'use client':
    - Props: meetingId, initialItems: ActionItem[], currentUserId
    - Local state: items (mirrors DB)
    - Add item: text input + "Add" button → POST /api/meetings/[id]/action-items → optimistically add to list
    - Check off: checkbox onChange → PATCH /api/meetings/[id]/action-items/[itemId] → write activity row action='action_item_completed'
    - Delete: X button → DELETE /api/meetings/[id]/action-items/[itemId]
    - Display: each item as a row with checkbox, text, due_date (if set), delete button

    Create /app/api/meetings/[id]/action-items/route.ts exporting POST:
    - Insert action_item, write activity row action='action_item_added'
    - Return new item

    Create /app/api/meetings/[id]/action-items/[itemId]/route.ts exporting PATCH and DELETE:
    - PATCH: update done + done_at, if done=true write activity row action='action_item_completed'
    - DELETE: delete the row

    Create status-changer.tsx as 'use client':
    - Props: meetingId, currentStatus, isOwner
    - Show current status badge
    - If isOwner: show status change buttons appropriate to current state:
      * want_to_meet → "Mark as Planned" button
      * planned → "Mark as Done" (confirm), "No Show" (confirm), "Cancel" (confirm)
      * done/no_show/cancelled → readonly status shown
    - Confirmation: use window.confirm() for destructive state changes (done/no_show/cancelled)
    - On confirm: PATCH /api/meetings/[id] with { status } → router.refresh()
  </action>
  <verify>
    npx tsc --noEmit --project "ceealar-pulse/tsconfig.json" 2>&1 | head -30
  </verify>
  <done>Meeting detail page renders all sections. Notes form autosaves. Action items add/check/delete work. Status changer with confirmation. TypeScript compiles.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. Migration file present: ls ceealar-pulse/supabase/migrations/0003_meetings.sql
2. Types exported: grep "export type Meeting" ceealar-pulse/lib/types.ts
3. Dialog component: ls "ceealar-pulse/app/(app)/attendees/_components/meeting-create-dialog.tsx"
4. Meeting detail page: ls "ceealar-pulse/app/(app)/meetings/[id]/page.tsx"
5. API routes: ls ceealar-pulse/app/api/meetings/
6. TypeScript compiles: npx tsc --noEmit --project ceealar-pulse/tsconfig.json
</verification>

<success_criteria>
- meetings, action_items, activity tables exist with correct schema + RLS
- meetings_view hides prep_note for non-owners pre-done
- Meeting create dialog wired to attendee detail page
- /meetings/[id] shows full meeting lifecycle
- Notes autosave on blur
- Action items add/check/delete with activity writes
- Status changer with confirmation modal
- TypeScript compiles clean
</success_criteria>
