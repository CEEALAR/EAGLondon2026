---
phase: 07-export-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ceealar-pulse/app/(app)/attendees/loading.tsx
  - ceealar-pulse/app/(app)/meetings/loading.tsx
  - ceealar-pulse/app/(app)/feed/loading.tsx
  - ceealar-pulse/app/(app)/attendees/page.tsx
  - ceealar-pulse/app/(app)/meetings/page.tsx
  - ceealar-pulse/app/(app)/feed/page.tsx
autonomous: true
requirements:
  - EXP-04
  - EXP-05

must_haves:
  truths:
    - "Navigating to /attendees shows a skeleton list of rows while data loads"
    - "Navigating to /meetings shows a skeleton timeline while data loads"
    - "Navigating to /feed shows skeleton activity rows while data loads"
    - "When the attendees table is empty, the page shows: 'No attendees imported yet — visit /admin/import'"
    - "When no meetings exist, the page shows: 'No meetings yet — find someone in Attendees and tap Schedule meeting'"
    - "When feed has no activity, the existing empty state message remains correct"
  artifacts:
    - path: "ceealar-pulse/app/(app)/attendees/loading.tsx"
      provides: "Next.js App Router loading.tsx for attendees route — skeleton rows"
    - path: "ceealar-pulse/app/(app)/meetings/loading.tsx"
      provides: "Next.js App Router loading.tsx for meetings route — skeleton timeline"
    - path: "ceealar-pulse/app/(app)/feed/loading.tsx"
      provides: "Next.js App Router loading.tsx for feed route — skeleton activity items"
    - path: "ceealar-pulse/app/(app)/attendees/page.tsx"
      provides: "Attendees page with empty state when attendees array is empty"
    - path: "ceealar-pulse/app/(app)/meetings/page.tsx"
      provides: "Meetings page with empty state when meetings array is empty"
  key_links:
    - from: "ceealar-pulse/app/(app)/attendees/loading.tsx"
      to: "ceealar-pulse/app/(app)/attendees/page.tsx"
      via: "Next.js App Router Suspense — loading.tsx is the automatic fallback"
      pattern: "loading.tsx"
    - from: "ceealar-pulse/app/(app)/attendees/page.tsx"
      to: "AttendeeList component"
      via: "passes attendees prop — empty state must be inside AttendeeList or before it"
      pattern: "attendees.length === 0"
---

## Phase Goal

**As a** CEEALAR team member, **I want to** see meaningful loading states and helpful empty states in all list views, **so that** the app feels polished and guides me when there's nothing to see yet.

<objective>
This plan delivers skeleton loading states (via Next.js loading.tsx convention) and contextual empty states for attendees, meetings, and feed. Both features touch the same files and have no dependencies on other Phase 7 plans.

Purpose: Polish pass that makes the app feel production-quality — no blank white screens or cryptic empty pages.
Output: Three loading.tsx files and updated empty state conditions in attendees/page.tsx, meetings/page.tsx (feed already has an empty state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Key patterns from existing pages. -->

From ceealar-pulse/app/(app)/attendees/page.tsx (current):
- Passes `attendees` (Attendee[]) to AttendeeList component
- attendees is [] when attendeesData is null/empty

From ceealar-pulse/app/(app)/meetings/page.tsx (current):
- Passes `meetings` (TimelineMeeting[]) to MeetingsTimeline
- meetings is [] when meetingsRaw is null/empty

From ceealar-pulse/app/(app)/feed/page.tsx (current):
- Already has empty state: groups.length === 0 renders "No activity yet..."
- groups is [] when activityRaw is empty

Brand CSS:
- Skeleton color: bg-muted (from shadcn — maps to ~#E5E7EB in light mode)
- Muted text: text-muted-foreground
- Teal link color: text-[var(--color-teal)]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton loading states (loading.tsx files)</name>
  <files>
    ceealar-pulse/app/(app)/attendees/loading.tsx
    ceealar-pulse/app/(app)/meetings/loading.tsx
    ceealar-pulse/app/(app)/feed/loading.tsx
  </files>
  <read_first>
    - ceealar-pulse/app/(app)/attendees/page.tsx (understand the visual structure to mirror in skeleton)
    - ceealar-pulse/app/(app)/meetings/page.tsx (understand the visual structure to mirror in skeleton)
    - ceealar-pulse/app/(app)/feed/page.tsx (understand the visual structure to mirror in skeleton)
  </read_first>
  <action>
    Create three loading.tsx files using the Next.js App Router convention. Each file exports a default function that returns a skeleton UI matching the visual structure of its page.

    All skeleton elements use: className="animate-pulse bg-muted rounded" with appropriate height/width classes.

    ATTENDEES loading.tsx:
    - Render a div with the same max-width and padding as the attendees list container
    - Show 12 skeleton rows. Each row: h-[72px] (matching the actual virtualized row height), full width, with rounded corners and animate-pulse. Use Array.from({ length: 12 }) to generate the rows without hardcoding 12 individual divs.
    - Add a search bar skeleton at the top: h-10 w-full rounded-md.

    MEETINGS loading.tsx:
    - Render a div with px-3 py-4 matching the meetings page wrapper
    - Show a skeleton heading (h-7 w-32) for "Schedule"
    - Show 3 day-tab skeletons (h-8 w-24 each in a flex row)
    - Below tabs: show 4 skeleton meeting blocks (h-16 w-full rounded) spaced with mb-3

    FEED loading.tsx:
    - Render a div with max-w-2xl mx-auto matching the feed wrapper
    - Show a heading skeleton (h-6 w-16 in px-4 py-4 wrapper)
    - Show 8 skeleton activity rows, each: px-4 py-3 with a h-4 w-3/4 line and below it a h-3 w-20 line (mimicking the two-line activity item structure)

    Do NOT import any existing components — these are pure HTML + Tailwind. No 'use client' needed (loading.tsx are Server Components by default).
  </action>
  <verify>
    <automated>cd "C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH/Claude/EAG London/ceealar-pulse" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ceealar-pulse/app/(app)/attendees/loading.tsx exists and exports a default function
    - ceealar-pulse/app/(app)/meetings/loading.tsx exists and exports a default function
    - ceealar-pulse/app/(app)/feed/loading.tsx exists and exports a default function
    - Each file contains "animate-pulse" class (grep confirms presence in all three files)
    - Each file contains "bg-muted" class (grep confirms)
    - No 'use client' directive in any loading.tsx file
    - TypeScript compiles with 0 errors
  </acceptance_criteria>
  <done>Three loading.tsx files exist; Next.js App Router automatically shows them as Suspense fallbacks during page data fetching.</done>
</task>

<task type="auto">
  <name>Task 2: Empty states for attendees and meetings</name>
  <files>
    ceealar-pulse/app/(app)/attendees/page.tsx
    ceealar-pulse/app/(app)/meetings/page.tsx
  </files>
  <read_first>
    - ceealar-pulse/app/(app)/attendees/page.tsx (full current content — understand where to add empty state check)
    - ceealar-pulse/app/(app)/meetings/page.tsx (full current content — understand where to add empty state check)
    - ceealar-pulse/app/(app)/feed/page.tsx (reference: existing empty state pattern uses groups.length === 0 check)
  </read_first>
  <action>
    Add empty state conditional renders to attendees/page.tsx and meetings/page.tsx.

    ATTENDEES page.tsx:
    After fetching attendees, before rendering AttendeeList, add a conditional:
    If attendees.length === 0, render an empty state div instead of AttendeeList:
    - Container: className="flex flex-col items-center justify-center py-20 px-4 text-center"
    - Primary text (p tag, text-base font-medium text-foreground): "No attendees imported yet"
    - Secondary text (p tag, text-sm text-muted-foreground mt-1): "Visit "
    - Include an anchor tag to /admin/import with className="text-[var(--color-teal)] underline underline-offset-2"
    - Full copy: "No attendees imported yet — visit /admin/import to upload the Swapcard spreadsheet."
    - The /admin/import link should be a Next.js Link component (import Link from 'next/link') wrapping just the path text, or an anchor — either works since this is a Server Component.

    MEETINGS page.tsx:
    The meetings page currently passes all meetings to MeetingsTimeline regardless. Add a check:
    After building the meetings array, if meetings.length === 0, render an empty state div instead of the full timeline:
    - Same container classes: flex flex-col items-center justify-center py-20 px-4 text-center
    - Keep the h1 "Schedule" heading visible above the empty state
    - Primary text (p tag, text-base font-medium text-foreground): "No meetings yet"
    - Secondary text (p tag, text-sm text-muted-foreground mt-1): "Find someone in Attendees and tap 'Schedule meeting' to add one."
    - Include a Link to /attendees with text "Browse Attendees" styled as text-[var(--color-teal)] underline underline-offset-2 on a separate line below the copy.

    Note: the meetings page query filters by date range (29–31 May 2026) — the empty state will show if no meetings have been scheduled yet OR if no meetings fall in the conference date range. The copy "No meetings yet" is appropriate for both cases.

    Do NOT modify AttendeeList or MeetingsTimeline components — all changes are in the Server Component pages.
  </action>
  <verify>
    <automated>cd "C:/Users/attil/OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH/Claude/EAG London/ceealar-pulse" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - attendees/page.tsx contains the string "No attendees imported yet" (grep confirms)
    - attendees/page.tsx contains a Link or anchor to "/admin/import" (grep confirms)
    - attendees/page.tsx contains "attendees.length === 0" condition (grep confirms)
    - meetings/page.tsx contains the string "No meetings yet" (grep confirms)
    - meetings/page.tsx contains "find someone in Attendees" or "Find someone in Attendees" (grep confirms, case-insensitive)
    - meetings/page.tsx contains a Link to "/attendees" (grep confirms)
    - meetings/page.tsx contains "meetings.length === 0" condition (grep confirms)
    - TypeScript compiles with 0 errors
  </acceptance_criteria>
  <done>Attendees and meetings pages show helpful, actionable empty states when data arrays are empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Skeleton and empty states are pure UI — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | Empty state copy | accept | Empty states reveal that /admin/import exists — this is acceptable since all users are authenticated @ceealar.org team members |
</threat_model>

<verification>
1. TypeScript: npx tsc --noEmit returns 0 errors across all modified files
2. Grep: grep -r "animate-pulse" ceealar-pulse/app/\(app\)/attendees/loading.tsx — matches
3. Grep: grep -r "animate-pulse" ceealar-pulse/app/\(app\)/meetings/loading.tsx — matches
4. Grep: grep -r "animate-pulse" ceealar-pulse/app/\(app\)/feed/loading.tsx — matches
5. Grep: grep "No attendees imported yet" ceealar-pulse/app/\(app\)/attendees/page.tsx — matches
6. Grep: grep "No meetings yet" ceealar-pulse/app/\(app\)/meetings/page.tsx — matches
</verification>

<success_criteria>
- Visiting /attendees during load shows animated skeleton rows (visible on slow network or CPU throttle in DevTools)
- Visiting /meetings during load shows animated skeleton timeline
- Visiting /feed during load shows animated skeleton activity rows
- With empty DB: /attendees shows "No attendees imported yet — visit /admin/import..."
- With no meetings: /meetings shows "No meetings yet — find someone in Attendees and tap 'Schedule meeting'"
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-polish/07-03-SUMMARY.md`
</output>
