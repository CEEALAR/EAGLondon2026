---
phase: 01-auth-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ceealar-pulse/.env.local
  - ceealar-pulse/lib/supabase/client.ts
  - ceealar-pulse/lib/supabase/server.ts
  - ceealar-pulse/middleware.ts
  - ceealar-pulse/app/auth/callback/route.ts
  - ceealar-pulse/app/(auth)/signin/page.tsx
  - ceealar-pulse/app/page.tsx
  - ceealar-pulse/supabase/migrations/0001_team_members.sql
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Visiting /signin shows a single 'Continue with Google' button"
    - "Clicking the button initiates Google OAuth restricted to @ceealar.org (hd param)"
    - "After OAuth success, callback verifies email ends with @ceealar.org; non-matching emails are signed out and redirected to /signin?error=unauthorized"
    - "On /signin?error=unauthorized the page shows 'This tool is for the CEEALAR team only.'"
    - "First @ceealar.org sign-in upserts a row in team_members with id, email, display_name, avatar_url from Google metadata"
    - "Visiting / when authenticated redirects to /attendees; when unauthenticated redirects to /signin"
    - "The migration SQL file exists and can be run in Supabase SQL editor to create team_members with RLS"
  artifacts:
    - path: "ceealar-pulse/lib/supabase/client.ts"
      provides: "Browser Supabase client via createBrowserClient"
      exports: ["createClient"]
    - path: "ceealar-pulse/lib/supabase/server.ts"
      provides: "Server Supabase client via createServerClient with cookies()"
      exports: ["createClient"]
    - path: "ceealar-pulse/middleware.ts"
      provides: "Session refresh middleware protecting all routes except /signin and /auth/callback"
      contains: "matcher"
    - path: "ceealar-pulse/app/auth/callback/route.ts"
      provides: "OAuth redirect handler with domain check and team_member upsert"
      exports: ["GET"]
    - path: "ceealar-pulse/app/(auth)/signin/page.tsx"
      provides: "Sign-in page with Google OAuth button and error message"
    - path: "ceealar-pulse/app/page.tsx"
      provides: "Root redirect: /attendees if authed, /signin if not"
    - path: "ceealar-pulse/supabase/migrations/0001_team_members.sql"
      provides: "team_members table DDL + RLS policies"
      contains: "create table public.team_members"
  key_links:
    - from: "app/(auth)/signin/page.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "browser client signInWithOAuth with queryParams.hd = 'ceealar.org'"
      pattern: "signInWithOAuth"
    - from: "app/auth/callback/route.ts"
      to: "team_members upsert"
      via: "supabase service role insert with on conflict do update"
      pattern: "from.*team_members.*upsert"
    - from: "middleware.ts"
      to: "protected routes"
      via: "supabase.auth.getUser() redirects unauthenticated requests to /signin"
      pattern: "getUser"
---

<objective>
Wire Supabase Auth end-to-end: browser client, server client, session-refresh middleware, OAuth callback with @ceealar.org domain enforcement, team_member upsert, sign-in page, and root redirect.

Purpose: Establish the authentication foundation that every subsequent phase depends on. A @ceealar.org user can sign in with Google and land in the app; any other account is rejected immediately.
Output: 8 files — env.local, two Supabase clients, middleware, callback route, signin page, root redirect page, migration SQL.
</objective>

<execution_context>
@C:\Users\attil\OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH\Claude\EAG London\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\attil\OneDrive - CENTRE FOR ENABLING EA LEARNING & RESEARCH\Claude\EAG London\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ceealar-pulse/app/layout.tsx
@ceealar-pulse/app/globals.css

<interfaces>
<!-- Installed packages relevant to this plan -->
<!-- @supabase/ssr ^0.10.3 — createBrowserClient, createServerClient -->
<!-- @supabase/supabase-js ^2.105.4 -->
<!-- next 15.5.18 — App Router, cookies() from next/headers -->

<!-- Environment variables (go in ceealar-pulse/.env.local, already in Vercel): -->
<!-- NEXT_PUBLIC_SUPABASE_URL=<from Vercel env vars> -->
<!-- NEXT_PUBLIC_SUPABASE_ANON_KEY=<from Vercel env vars> -->
<!-- SUPABASE_SERVICE_ROLE_KEY=<from Vercel env vars — server-side only> -->

<!-- Root layout (already exists — DO NOT modify in this plan): -->
<!-- app/layout.tsx wraps children in DM Sans + Playfair Display fonts -->

<!-- CSS custom properties available: --color-teal: #0F766E; --color-gold: #D4A017; --color-cream: #FAF7F0 -->

<!-- Route group convention: -->
<!--   (auth) = public routes, no nav shell -->
<!--   (app)  = protected routes, with nav shell (added in Plan 01-02) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase clients, .env.local, and middleware</name>
  <files>
    ceealar-pulse/.env.local
    ceealar-pulse/lib/supabase/client.ts
    ceealar-pulse/lib/supabase/server.ts
    ceealar-pulse/middleware.ts
  </files>
  <read_first>
    ceealar-pulse/app/layout.tsx
    ceealar-pulse/package.json
  </read_first>
  <action>
Create ceealar-pulse/.env.local with the three Supabase environment variables. This file must be gitignored (check .gitignore; add .env.local entry if missing).

Create ceealar-pulse/lib/supabase/client.ts — browser-safe Supabase client. Export a single function createClient() that calls createBrowserClient from @supabase/ssr with process.env.NEXT_PUBLIC_SUPABASE_URL and process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY. This file must NEVER import from next/headers and must NEVER reference SUPABASE_SERVICE_ROLE_KEY.

Create ceealar-pulse/lib/supabase/server.ts — server-only Supabase client. Export an async function createClient() that calls createServerClient from @supabase/ssr. Pass NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY. The cookies adapter must call (await cookies()).get(name) for getAll and (await cookies()).set(name, value, options) for setAll — Next.js 15 cookies() is async. Add 'use server' directive at the top or rely on the fact that this module is only imported in server components and route handlers.

Create ceealar-pulse/middleware.ts at the project root (not inside app/). The middleware must:
1. Import createServerClient from @supabase/ssr and NextResponse from next/server.
2. Create a supabase client using the request cookies adapter pattern from @supabase/ssr middleware docs: getAll reads from request.cookies.getAll(), setAll writes to both request and response cookies.
3. Call supabase.auth.getUser() to refresh the session.
4. If getUser returns no user AND the pathname is NOT /signin AND NOT /auth/callback: redirect to /signin.
5. If getUser returns a user AND pathname is /signin: redirect to /attendees.
6. Export a config with matcher that excludes _next/static, _next/image, favicon.ico, and *.{svg,png,jpg,jpeg,gif,webp} — protect everything else including / and all app routes.

The matcher regex: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']
  </action>
  <verify>
    <automated>cd ceealar-pulse && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- ceealar-pulse/.env.local contains NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
- lib/supabase/client.ts exports createClient(), uses createBrowserClient, no server-only imports
- lib/supabase/server.ts exports async createClient(), uses createServerClient, awaits cookies()
- middleware.ts exports default middleware function and config.matcher array
- TypeScript compiles without errors on these four files
  </acceptance_criteria>
  <done>Four files exist, TypeScript reports no errors, .env.local has all three env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Auth callback route with domain check and team_member upsert</name>
  <files>
    ceealar-pulse/app/auth/callback/route.ts
    ceealar-pulse/supabase/migrations/0001_team_members.sql
  </files>
  <read_first>
    ceealar-pulse/lib/supabase/server.ts
  </read_first>
  <action>
Create ceealar-pulse/app/auth/callback/route.ts as a Next.js App Router route handler.

The GET handler must:
1. Accept a NextRequest. Extract the code search param: request.nextUrl.searchParams.get('code').
2. If code is present: call supabase.auth.exchangeCodeForSession(code) using the server client.
3. After exchange, call supabase.auth.getUser() to get the authenticated user.
4. Check user.email — if it does NOT end with '@ceealar.org':
   a. Call supabase.auth.signOut()
   b. Return NextResponse.redirect to the app origin + '/signin?error=unauthorized'
5. If domain check passes, upsert into team_members using the service-role client (import createClient from lib/supabase/server but call it with the service role key, OR use the anon client since the upsert runs server-side after auth — use the anon client with the authenticated session, which satisfies the RLS insert policy via auth.uid(). NOTE: RLS only has select and update policies, not insert. To upsert team_members server-side without RLS blocking, create a second server client using SUPABASE_SERVICE_ROLE_KEY — create this inline in the route using createServerClient with the service_role key and a dummy cookie store (empty getAll, no-op setAll).
6. The upsert SQL equivalent: insert into team_members (id, email, display_name, avatar_url) values (user.id, user.email, user.user_metadata.full_name, user.user_metadata.avatar_url) on conflict (id) do update set display_name = excluded.display_name, avatar_url = excluded.avatar_url. Use supabase .from('team_members').upsert({...}, { onConflict: 'id' }).
7. Redirect to the app origin + '/attendees' on success.
8. If no code param: redirect to '/signin'.

For the service-role client inline in the callback route, use:
  const adminClient = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )
Import createServerClient directly from @supabase/ssr for this inline usage.

Create ceealar-pulse/supabase/migrations/0001_team_members.sql with the exact SQL:

  create table public.team_members (
    id uuid primary key references auth.users(id) on delete cascade,
    email text not null unique,
    display_name text,
    avatar_url text,
    created_at timestamptz default now()
  );

  alter table public.team_members enable row level security;

  create policy "team members can view all"
    on public.team_members for select
    using (auth.uid() is not null);

  create policy "users can update own record"
    on public.team_members for update
    using (auth.uid() = id);
  </action>
  <verify>
    <automated>cd ceealar-pulse && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- app/auth/callback/route.ts exports GET and handles: code exchange, domain check, signOut for non-@ceealar.org, team_members upsert using service role client, redirect to /attendees
- Non-@ceealar.org path: signOut called, redirect to /signin?error=unauthorized
- supabase/migrations/0001_team_members.sql contains create table, enable row level security, and both create policy statements exactly as specified
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>Callback route handles all three cases (no code, wrong domain, success). Migration SQL file exists with correct DDL.</done>
</task>

<task type="auto">
  <name>Task 3: Sign-in page and root redirect</name>
  <files>
    ceealar-pulse/app/(auth)/signin/page.tsx
    ceealar-pulse/app/page.tsx
  </files>
  <read_first>
    ceealar-pulse/lib/supabase/client.ts
    ceealar-pulse/lib/supabase/server.ts
    ceealar-pulse/app/globals.css
  </read_first>
  <action>
Create ceealar-pulse/app/(auth)/signin/page.tsx. This is a client component ('use client').

The page must:
1. Import useSearchParams from next/navigation to detect ?error=unauthorized.
2. Import createClient from @/lib/supabase/client (browser client).
3. Render a centered full-screen layout with background-color var(--color-cream).
4. Show the CEEALAR Pulse wordmark: "CEEALAR Pulse" in font-display (Playfair Display italic) at text-2xl, color #0F766E.
5. Below it, a subtitle: "Conference coordination" in DM Sans text-sm text-muted-foreground.
6. If searchParams has error=unauthorized, show an alert box: "This tool is for the CEEALAR team only." in text-sm with a red/destructive color, rendered above the button.
7. A "Continue with Google" button — use the shadcn Button component (import from @/components/ui/button). The button should be full-width max-w-xs, variant default, with a Google SVG icon inline (simple colored G logo: a 16x16 SVG with the letter G in Google colors, or a plain Google wordmark). On click, call:
   supabase.auth.signInWithOAuth({
     provider: 'google',
     options: {
       redirectTo: window.location.origin + '/auth/callback',
       queryParams: { hd: 'ceealar.org' }
     }
   })
8. Layout: flex flex-col items-center justify-center min-h-screen gap-6 px-4.

Create ceealar-pulse/app/page.tsx. This is a server component. It must:
1. Import createClient from @/lib/supabase/server.
2. Call const supabase = await createClient() then const { data: { user } } = await supabase.auth.getUser().
3. If user exists: import redirect from next/navigation and call redirect('/attendees').
4. If no user: call redirect('/signin').
5. The component body after the redirects can return null (redirects always throw in Next.js).
  </action>
  <verify>
    <automated>cd ceealar-pulse && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- app/(auth)/signin/page.tsx is a 'use client' component with useSearchParams, Google OAuth button, error message conditional on ?error=unauthorized
- Button calls signInWithOAuth with provider 'google', redirectTo pointing to /auth/callback, queryParams.hd = 'ceealar.org'
- app/page.tsx is a server component that checks auth and redirects to /attendees or /signin
- npm run dev starts without errors (run: cd ceealar-pulse && npm run build 2>&1 | tail -20)
  </acceptance_criteria>
  <done>Sign-in page renders with brand styling and Google button. Root page redirects based on auth state. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Auth | OAuth initiation; untrusted until callback verifies session |
| OAuth callback → app server | code param from Google via Supabase; must exchange before trusting |
| Server route → Supabase DB | service role key used only server-side in route handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | middleware.ts | mitigate | getUser() called on every request; unauthenticated requests redirected to /signin before reaching any app route |
| T-01-02 | Elevation of Privilege | app/auth/callback/route.ts | mitigate | Email domain check after exchangeCodeForSession; non-@ceealar.org triggers signOut + redirect to /signin?error=unauthorized |
| T-01-03 | Information Disclosure | lib/supabase/client.ts | mitigate | SUPABASE_SERVICE_ROLE_KEY never imported in client.ts; only NEXT_PUBLIC_ vars used; service role key used only in server route handler |
| T-01-04 | Spoofing | OAuth callback CSRF | accept | Supabase uses PKCE flow; the code verifier is tied to the originating session; no additional CSRF token needed |
</threat_model>

<verification>
After all three tasks complete, verify the end-to-end slice:

1. `cd ceealar-pulse && npm run build` — must succeed with no TypeScript errors and no missing module errors.
2. Confirm file tree: lib/supabase/client.ts, lib/supabase/server.ts, middleware.ts, app/auth/callback/route.ts, app/(auth)/signin/page.tsx, app/page.tsx, supabase/migrations/0001_team_members.sql all exist.
3. Grep check — service role key must not appear in client.ts: `grep -c "SERVICE_ROLE" ceealar-pulse/lib/supabase/client.ts` must return 0.
4. Grep check — hd param present in signin page: `grep -c "hd.*ceealar.org" ceealar-pulse/app/(auth)/signin/page.tsx` must return 1 or more.
5. Grep check — domain check in callback: `grep -c "ceealar.org" ceealar-pulse/app/auth/callback/route.ts` must return 1 or more.
6. .env.local exists: `test -f ceealar-pulse/.env.local && echo exists`.
</verification>

<success_criteria>
- `npm run build` exits 0 with no TypeScript or module errors
- All 7 files exist in the correct locations
- SUPABASE_SERVICE_ROLE_KEY is absent from client.ts (server-only secret boundary enforced)
- hd: 'ceealar.org' present in signin OAuth call (domain restriction at OAuth layer)
- @ceealar.org domain check present in callback route (application layer restriction)
- Migration SQL contains team_members table + RLS + both policies
- The plan addresses: AUTH-01 (Google OAuth button), AUTH-02 (hd restriction), AUTH-03 (rejection message + signOut), AUTH-04 (team_member upsert)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-shell/01-01-SUMMARY.md` using the summary template. Include:
- Files created (all 8)
- Key decisions: service role client inline in callback, Next.js 15 async cookies(), PKCE flow accepted for CSRF
- Patterns: createBrowserClient in client.ts, createServerClient with cookies adapter in server.ts, same createServerClient with empty cookie store for service role
- Any deviations from this plan
</output>
